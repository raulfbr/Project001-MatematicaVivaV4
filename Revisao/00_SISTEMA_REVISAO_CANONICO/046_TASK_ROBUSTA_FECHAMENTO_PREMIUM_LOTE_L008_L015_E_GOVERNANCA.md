# TASK ROBUSTA - FECHAMENTO PREMIUM DO LOTE L008-L015 E GOVERNANCA VIVA
Data: 2026-03-16
Status: pronta para execucao disciplinada
Escopo: consolidar o fechamento premium do lote `L008-L015`, blindar a governanca viva da fase e deixar rastreabilidade honesta para retomadas futuras

---

## 1) Missao desta task
1. garantir que o lote `L008-L015` permaneceu premium no produto publicado, sem regressao silenciosa;
2. fechar pendencias de governanca que ainda contam uma historia antiga sobre o estado do projeto;
3. separar com clareza:
   a. o que ja esta resolvido no HTML;
   b. o que ainda depende de validacao humana real;
   c. o que continua sendo divida documental ou curricular, e nao blocker de publicacao.

---

## 2) O que esta task NAO deve fazer
1. nao reabrir HTML de `L008-L015` por ansiedade estetica;
2. nao forcar reescrita narrativa em licoes que ja fecharam em `PASS PREMIUM`;
3. nao promover YAML cru a fonte principal quando o HTML publicado esta mais maduro;
4. nao transformar logs em duplicacao infinita da governanca central.

---

## 3) Diagnostico consolidado do lote
1. `L008-L015` estao limpas no HTML publicado:
   a. sem backticks literais residuais;
   b. sem `journey-connection-card`, `lesson-footer-nav`, `lesson-footer-link`, `ritual-whisper-box`, `Sussurro do Portal` ou `Lugar revelado`;
   c. com navegacao funcional nas paginas auditadas.
2. os logs `2026-03-16` de `L008-L015` estao em `UTF-8` sem `BOM`.
3. o principal passivo remanescente saiu do HTML e ficou em governanca:
   a. `Revisao/README.md` ainda apontava `015` como leitura principal atual;
   b. `000_INDEX_SISTEMA_REVISAO.md` ainda reproduzia a mesma hierarquia antiga;
   c. o indice ainda nao refletia bem o surgimento do `045` e desta task de fechamento;
   d. os YAMLs das licoes seguem mais crus do que o produto publicado.

---

## 4) O que precisa ser aplicado nesta rodada
### A. Governanca viva
1. alinhar `Revisao/README.md` com `AI_CONTEXT.md`:
   a. leitura principal atual = `AI_CONTEXT.md` + `005_STATUS_REVISAO_SEMENTES.md`;
   b. `015` = apoio historico util.
2. alinhar `000_INDEX_SISTEMA_REVISAO.md` com a mesma hierarquia.
3. garantir que o indice cite:
   a. `045_PLANO_DETALHADO_REVISAO_PROFUNDA_L008_O_PAR_PERFEITO.md`;
   b. `046_TASK_ROBUSTA_FECHAMENTO_PREMIUM_LOTE_L008_L015_E_GOVERNANCA.md`.

### B. Rastreabilidade honesta
1. manter logs `L008-L015` como retrato fiel do que realmente foi feito;
2. evitar claims de "impecavel" quando o que resta e validacao humana ou divida documental;
3. manter nomes reais dos arquivos com acentos corretos.

### C. Fechamento premium do lote
1. registrar explicitamente que o HTML do lote esta limpo;
2. registrar que o proximo gate real nao e novo patch de IA, e sim:
   a. validacao com familia real;
   b. eventual sincronizacao curricular dos YAMLs;
   c. manutencao da governanca como SSOT.

---

## 5) Checklist de execucao
1. revisar `Revisao/README.md`;
2. revisar `Revisao/00_SISTEMA_REVISAO_CANONICO/000_INDEX_SISTEMA_REVISAO.md`;
3. inserir referencias a `045` e `046` no indice;
4. confirmar que `L008-L015` continuam limpas por busca de residuos conhecidos;
5. confirmar `UTF-8` sem `BOM` nos logs e nos HTMLs do lote;
6. encerrar a rodada com veredito sincero:
   a. `PASS PREMIUM` no produto publicado;
   b. `governanca viva ajustada`;
   c. `validacao humana pendente`.

---

## 6) Criterios de aceite
1. nenhum arquivo central da revisao deve continuar dizendo que `015` e a leitura principal atual do estado do projeto;
2. o indice precisa refletir `045` e `046`;
3. o lote `L008-L015` deve continuar limpo no HTML;
4. a resposta final da rodada precisa distinguir:
   a. produto publicado;
   b. governanca;
   c. risco residual honesto.

---

## 7) Risco residual honesto
1. validacao humana com familias reais segue sendo o principal gate fora da IA;
2. YAMLs `L008-L015` ainda podem ficar atras do HTML como retrato do produto final;
3. algum ruido de encoding pode continuar aparecendo no terminal, mesmo com os arquivos corretos em `UTF-8`.

---

## 8) Veredito esperado desta task
1. `produto publicado do lote L008-L015`: `PASS PREMIUM`;
2. `governanca viva da fase`: `PASS COM REFINO`;
3. `fechamento geral`: pronto para transicao da IA de "caça a sobra" para "manutencao disciplinada + validacao humana".
