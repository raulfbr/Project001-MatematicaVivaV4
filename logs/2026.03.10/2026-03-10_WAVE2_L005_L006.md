# WAVE 2 - L005 E L006
Data: 2026-03-10
Escopo: `MV-S-005_O_ESCONDERIJO_DA_GLÓRIA.html` e `MV-S-006_O_DESENHO_DO_REI.html`
Status: passada estrutural intermediaria concluida

## Estado de entrada
1. `L005-L006` estavam em modelo legado com labels antigas, `div onclick`, ausencia de `Sementes para o Dia` e bloco final ainda nomeado como `Para a Familia`.
2. havia risco de drift forte em relacao ao contrato hoje consolidado em `023`.

## Patch aplicado
### `L005`
1. hero refinado com quote e `alt` melhores;
2. `Foco` legado convertido em formula mais canonica;
3. `Ritual de Abertura` convertido em `Ritual de Entrada`;
4. `Atividade Concreta` convertido em `O Concreto`;
5. CTA de `Conexao da Jornada` migrado de `div onclick` para link semantico;
6. `Para a Familia` convertido em `Formacao do Portador`;
7. `Sementes para o Dia` inserido antes da formacao;
8. abertura de `Formacao do Portador` reancorada em `Estrategia do Mestre`.

### `L006`
1. hero refinado com quote e `alt` melhores;
2. `Foco` legado convertido em formula mais canonica;
3. `Ritual de Abertura` convertido em `Ritual de Entrada`;
4. `Atividade Concreta` convertido em `O Concreto`;
5. CTA de `Conexao da Jornada` migrado de `div onclick` para link semantico;
6. `Para a Familia` convertido em `Formacao do Portador`;
7. `Sementes para o Dia` inserido antes da formacao;
8. abertura de `Formacao do Portador` reancorada em `Estrategia do Mestre`.

## Verificacao rapida
1. sem `Ritual de Abertura`, `Atividade Concreta`, `Para a Familia`, `[Foco:` ou `div onclick` em `L005-L006`;
2. `Ritual de Entrada`, `O Concreto`, `Sementes para o Dia` e `Formacao do Portador` agora aparecem nas duas paginas;
3. sem sinais visiveis de corrupcao `Ã`, `Â`, `â`, `ðŸ`, `Ãƒ`, `Ã‚` ou `Ã¢` nas checagens rapidas.

## Risco residual
1. esta ainda nao foi a reescrita premium integral que `L003-L004` receberam;
2. `L005-L006` continuam misturando markup legado com naming novo;
3. a proxima passada precisa aprofundar `Taste`, fronteiras topicas e maior coerencia visual/semantica.
